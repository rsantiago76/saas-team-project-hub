export default function Workspaces() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Workspaces</h1>
      <p>Build fix applied (providers imports outputs via relative path).</p>
    </main>
  );
}
